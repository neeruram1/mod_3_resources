<section id="resource-<%= resource.id %>">
  <p><%= resource.title %>
  <p><%= resource.link %>
  <p><%= resource.source %>
  <p><%= resource.category %>
  <p><%= resource.project %>
  <%= link_to 'Delete Resource', "/resources/#{resource.id}", method: :delete %>
 </section>
